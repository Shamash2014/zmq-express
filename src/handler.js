// Event -> State -> State
const handler = (event, state) => {
  // will return new state or initial
  // akin to reducers
  switch (event) {
    case 'START':
      state;
      break;
  }
};

module.exports = { handler };
